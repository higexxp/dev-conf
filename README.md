# dev-conf

# plugin manager
brew install sheldon
sheldon init --shell zsh <- いらない

.zshrc
eval "$(sheldon source)"

brew install --cask maccy

brew install starship

brew install git-graph tig
brew install tmux
brew install nvim

git clone https://github.com/tmux-plugins/tpm ~/.tmux/plugins/tpm

vscode font
MesloLGS NF

